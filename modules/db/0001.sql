CREATE TABLE BIKECOMPUTER_LOG(
  timestamp DATETIME,
  lap INTEGER,
  timer INTEGER,
  total_timer_time INTEGER,
  elapsed_time INTEGER,
  position_lat FLOAT,
  position_long FLOAT,
  raw_lat FLOAT,
  raw_long FLOAT,
  gps_altitude FLOAT,
  gps_speed FLOAT,
  gps_distance FLOAT,
  gps_mode INTEGER,
  gps_used_sats INTEGER,
  gps_total_sats INTEGER,
  gps_track INTEGER,
  gps_epx FLOAT,
  gps_epy FLOAT,
  gps_epv FLOAT,
  gps_pdop FLOAT,
  gps_hdop FLOAT,
  gps_vdop FLOAT,
  heart_rate INTEGER,
  cadence INTEGER,
  distance FLOAT,
  speed FLOAT,
  power INTEGER,
  accumulated_power INTEGER,
  temperature FLOAT,
  pressure FLOAT,
  humidity INTEGER,
  altitude FLOAT,
  course_altitude FLOAT,
  heading INTEGER,
  motion INTEGER,
  acc_x FLOAT,
  acc_y FLOAT,
  acc_z FLOAT,
  gyro_x FLOAT,
  gyro_y FLOAT,
  gyro_z FLOAT,
  light INTEGER,
  cpu_percent INTEGER,
  total_ascent FLOAT,
  total_descent FLOAT,
  lap_heart_rate INTEGER,
  lap_cadence INTEGER,
  lap_distance FLOAT,
  lap_speed FLOAT,
  lap_power INTEGER,
  lap_accumulated_power INTEGER,
  lap_total_ascent FLOAT,
  lap_total_descent FLOAT,
  avg_heart_rate INTEGER,
  avg_cadence INTEGER,
  avg_speed FLOAT,
  avg_power INTEGER,
  lap_cad_count INTEGER,
  lap_cad_sum INTEGER,
  avg_cad_count INTEGER,
  avg_cad_sum INTEGER,
  lap_power_count INTEGER,
  lap_power_sum INTEGER,
  avg_power_count INTEGER,
  avg_power_sum INTEGER
);

CREATE INDEX lap_index ON BIKECOMPUTER_LOG(lap);
CREATE INDEX total_timer_time_index ON BIKECOMPUTER_LOG(total_timer_time);
CREATE INDEX timestamp_index ON BIKECOMPUTER_LOG(timestamp);
